<UserControl x:Class="SJBCS.Students.StudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:codeDisplayer="clr-namespace:CodeDisplayer;assembly=CodeDisplayer"
             xmlns:student="clr-namespace:SJBCS.Students"
             xmlns:local="clr-namespace:SJBCS.Students"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type student:StudentListViewModel}">
            <student:StudentListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type student:StudentInfoViewModel}">
            <student:StudentInfoView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid Width="800">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ContentControl Content="{Binding StudentList}"
                        Grid.Column="0"/>
        <ContentControl Content="{Binding StudentInfo}"
                        Grid.Column="1" />

    </Grid>
</UserControl>
